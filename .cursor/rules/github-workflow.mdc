---
description: ContextBridge Agent 项目 Git 推送补充（通用流程见全局 Skill）
alwaysApply: true
---

# ContextBridge Agent — Git 推送项目补充

通用两阶段门禁见全局 Skill：`~/.cursor/skills/github-push-workflow/SKILL.md`（口令「提交并 push」时读取）。

## 本项目默认

- 默认远程：`fireloop0828/contextbridge-agent`（私有）
- 默认分支：`main`
- 首次 push 可用：`git push -u origin main`

## 安全补充

- 禁止提交：`rag-server/config/settings.yaml`（含真实 api_key 时）
- 提交前扫描 `config.json` 的 MCP `env` 不得含明文 Key

## 模块查漏表（辅助分批，非机械拆分依据）

| 意图类型 | 常见路径 |
|----------|----------|
| 旅行模式 | `modes/travel/**` |
| 记忆与会话 | `session_store.py`、`ui/sidebar.py` |
| UI / 聊天 | `ui/chat.py`、`ui/sidebar.py` |
| MCP / 配置 | `config/`、`mcp_server_*.py`、`config.json` |
| RAG 子项目 | `rag-server/**`（与 agents-master 不同意图时再拆） |
| 文档 | `docs/**`（与功能同意图则同 commit） |
| 脚本 | `scripts/**` |
